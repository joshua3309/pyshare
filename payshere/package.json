{
  "name": "paysphere",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:web": "npm run dev --workspace web",
    "dev:admin": "npm run dev --workspace admin",
    "build:web": "npm run build --workspace web",
    "build:admin": "npm run build --workspace admin",
    "build": "npm run build:web && npm run build:admin",
    "lint": "npm run lint --workspace web && npm run lint --workspace admin",
    "typecheck": "npm run typecheck --workspace web && npm run typecheck --workspace admin"
  },
  "devDependencies": {
    "typescript": "5.2.2"
  }
}
